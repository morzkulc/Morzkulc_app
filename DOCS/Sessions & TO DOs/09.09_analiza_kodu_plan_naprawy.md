# Analiza kodu SKK Morzkulc — wydajność, poprawność, duplikacje, martwy kod

**Data:** 09.09.2026
**Zakres:** cały backend (`functions/src/**`) + cały frontend (`public/**`) + warstwa danych (indeksy Firestore, zależności, higiena repo, pokrycie testami).
**Charakter:** WYŁĄCZNIE analiza — żadna linijka kodu nie została zmieniona. Poniżej wyniki + plan napraw do realizacji na życzenie.
**Metoda:** build + lint + pełne uruchomienie testów (baza obiektywna), plus pełny odczyt kodu źródłowego (nie tylko mapy `_claude_context`) w 4 równoległych, niezależnych przejściach: (1) backend/API, (2) backend/logika biznesowa+joby, (3) frontend, (4) przekrój dane/indeksy/zależności/testy. Każde znalezisko zweryfikowane grepem/odczytem aktualnego kodu — stary `ai_full_audit_report.txt/json` (kwiecień, inna gałąź, narzędzie już nie istnieje) **pominięty jako nieaktualny**, nigdzie nie cytowany jako źródło.
**Ograniczenia:** brak dostępu do konsoli GCP/Firebase i logów produkcyjnych w tej sesji — kilka ustaleń dot. indeksów Firestore oznaczono jako „do zweryfikowania w konsoli”, nie jako pewny fakt.

---

## 0. Wynik bazowy (stan na dziś, zweryfikowany uruchomieniem)

| Sprawdzenie | Wynik |
|---|---|
| `npm --prefix functions run build` (tsc) | ✅ 0 błędów |
| `npm --prefix functions run lint` (eslint) | ✅ 0 błędów/ostrzeżeń |
| `npm --prefix functions run test` (vitest, 7 plików / 125 testów) | ⚠️ **124 ✅ / 1 ❌** — patrz niżej |
| `python -m pytest tests/test_bundle_reservations.py tests/test_godzinki.py tests/test_pwa.py` (248 testów) | ✅ 248/248 — ale to testy-lustrzane (logika przepisana na Python), nie wykonują realnego kodu TS; ryzyko cichej rozbieżności z produkcją |

**Jedyny czerwony test — zdiagnozowany, NIE jest bugiem produkcyjnym:**
`functions/test/gear_core.test.ts` → `quoteKayaksCostHours` → *„zarząd/KR darmowo tylko przy boardDoesNotPay=true”* oczekuje `0`, dostaje `20`.
Przyczyna: `functions/src/modules/hours/hours_quote.ts::quoteKayaksCostHours` **celowo** (komentarz w kodzie, linie 8–13) przestało zwracać `0` dla zarządu/KR — zawsze liczy realny koszt, a zwolnienie z opłaty jest teraz obsługiwane w `gear_bundle_service.ts` przez mechanizm `boardFeeExempt`/`waived: true` (koszt zapisany, ale przekreślony w historii, saldo bez zmian). Zweryfikowano, że mechanizm `waived` jest konsekwentnie zaimplementowany we wszystkich 3 ścieżkach: tworzenie rezerwacji (`gear_bundle_service.ts:650,731,777-783`), edycja dat (`:882,984-1030`), anulowanie/zwrot (`godzinki_service.ts::refundHoursForReservationInTx`). **Test jest przestarzały wobec świadomie zmienionego kontraktu — do aktualizacji, nie do „naprawy” logiki.**

---

## 1. Executive summary — najważniejsze ustalenia

| # | Obszar | Problem | Dlaczego ważne | Priorytet |
|---|---|---|---|---|
| 1 | Dane | Brak composite index `gear_reservations(userUid, createdAt)` dla „moje rezerwacje” (`gear_bundle_service.ts::listMyBundleReservations`) | Dotyczy **każdego** zalogowanego użytkownika (moduł `my_reservations` jest auto-wstrzykiwany); realne ryzyko `FAILED_PRECONDITION` w produkcji | **Wysoki** |
| 2 | Dane | Brak composite index `service_jobs(status, nextRunAt)` dla `fallbackDailyWorker.ts` | Fallback dobijający zawieszone joby może sam nigdy się nie wykonać poprawnie | **Wysoki** |
| 3 | Backend/joby | Job, który „zawiśnie” w statusie `running` (timeout/restart instancji), **nigdy nie jest odzyskiwany** — `lockedUntil` jest polem martwym | Cichy, trwały brak wykonania zadania bez alertu — realne ryzyko dziś | **Wysoki** |
| 4 | Backend/joby | Brak cache tokena OAuth w `googleAuth.ts::getDelegatedAuth` — każda operacja Workspace/Sheets/Calendar robi pełną wymianę tokena od nowa | Mnoży liczbę żądań sieciowych w każdym tasku synchronizacji/powiadomień o rząd wielkości, zwiększa ryzyko timeoutu i zawieszenia jobów (#3) | **Wysoki** |
| 5 | Backend/API | Podwójny pełny skan `users_opening_balance_26` w `registerUserHandler.ts` (`findOpeningBalance` + `emailExistsInOtherObRow`) | Wykonywane przy **każdym logowaniu/rejestracji** — najgorętsza ścieżka aplikacji | **Wysoki** |
| 6 | Frontend | `.gearBadges` ma `display:none` jako regułę bazową w `gear.css:214-219` — plakietka „Basen” i inne **nigdy się nie renderują**, na żadnym ekranie | Jedyny znaleziony realny, widoczny dla użytkownika bug UI (nie tylko techniczny dług) | **Wysoki** |
| 7 | Frontend | `index.html` ładuje `app.css`, który przez `@import` ściąga wszystkie 9 arkuszy CSS (~113 KB) na każdej stronie, dla każdej roli | Spowalnia pierwszy render na słabym łączu; `@import` w CSS blokuje renderowanie sekwencyjnie zamiast równolegle | **Wysoki** |
| 8 | Frontend | `assets/kayak-placeholder.png` waży 1,4 MB jako placeholder każdej karty sprzętu | Najtańsza poprawka wydajności w całym audycie — podmiana jednego pliku | **Wysoki** |
| 9 | Backend/joby | Ryzyko podwójnej wysyłki maili przy retry jobu (`basenNotifySessionCancelled.ts`, `gearNotifyReservationCancelledByAdmin.ts`, `usersSyncFunctionRolesFromSetup.ts`) — brak znacznika „już wysłano” | Konkretny, odtwarzalny scenariusz: członek klubu dostaje 2-3 identyczne maile | **Wysoki** |
| 10 | Backend/dane | `gear_reservations` nigdy nie zmienia statusu na `completed` po zakończeniu terminu — każda nowa rezerwacja skanuje **całą historię** aktywnych rezerwacji od początku istnienia modułu | Koszt rośnie bezpowrotnie z każdym rokiem działania klubu; już dziś obciąża każdą transakcję rezerwacji | **Wysoki** |
| 11 | Testy | 61/62 (98%) handlerów `api/` i 27/34 (79%) tasków bez jakiegokolwiek testu jednostkowego; całe moduły **basen** i **km** oraz rdzeń limitów/konfliktów rezerwacji sprzętu (`gear_bundle_service.ts`, `reservation_limits.ts`) — 0% pokrycia | Obszar sprzętu ma udokumentowaną w pamięci projektu historię wielu bugfixów (limity per-kategoria, duplikaty ID) — zmiany tam dziś są bez siatki bezpieczeństwa | **Wysoki (długoterminowo)** |
| 12 | Higiena repo | 14 MB martwego, zdublowanego katalogu `.claude_context/` trackowanego w git (zastąpiony przez aktywny `_claude_context/`) + 596 KB nieaktualnego `ai_full_audit_report.*` | Zaśmieca repo/klony, myli — dwa równoległe narzędzia do tego samego celu | Średni |

Pełna lista **96 zweryfikowanych znalezisk** w sekcjach 2–5 poniżej.

---

## 2. Backend — warstwa HTTP API (`functions/src/index.ts` + `functions/src/api/*.ts`, 61 plików, 10 084 linii)

Zweryfikowano też pełną zgodność `index.ts` ↔ `firebase.json` (rewrites): **65/65 zgodnych**, brak martwych/nieosiągalnych endpointów poza jednym niepodpiętym handlerem (patrz M1).

### 2.1 Wydajność

| # | Plik:linia | Problem | Dlaczego to problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|---|
| W1 | `getAdminPendingHandler.ts:305-318` | Pętla po prywatnych kajakach z sekwencyjnym `await` zapytania o właściciela w środku | Klasyczny N+1 — czas odpowiedzi panelu rośnie liniowo z liczbą kajaków | Zebrać `ownerContact` przed pętlą, rozwiązać właścicieli jednym `Promise.all`/`getAll`, iterować w pamięci. 1 plik | Wysoki |
| W2 | `registerUserHandler.ts:241,285,630-641,821-829` | `findOpeningBalance` i `emailExistsInOtherObRow` osobno robią pełny skan `users_opening_balance_26`; obie wołane w tym samym request | Dwukrotny pełny odczyt tej samej kolekcji przy **każdym** logowaniu/rejestracji | `findOpeningBalance` ma zwracać już pobrany snapshot i przekazywać go do `emailExistsInOtherObRow`. 1 plik, 2 funkcje | Wysoki |
| W3 | `registerUserHandler.ts:766-772,920-923` | `resolveSzkoleniowiec`, `resolveKierownikSummary`, `resolveBasenAdminGrant` wołane sekwencyjnie mimo niezależności | Endpoint `/api/register` wykonuje się przy każdym logowaniu — każdy zbędny round-trip liczy się | Zamienić na `Promise.all([...])`. 1 plik | Średni |
| W4 | `getAdminMemberDuesHandler.ts:91` | `db.collection("godzinki_ledger").get()` bez filtra/limitu — cała kolekcja do pamięci | Kolekcja rośnie bezterminowo z historią klubu | Docelowo: zdenormalizowane saldo per-user (patrz Faza 3). Krótkoterminowo: rozważyć filtr/limit | Średni |
| W5 | `getBasenAdminGodzinyUsersHandler.ts:86` | `basen_godziny_ledger.get()` — pełny skan całej kolekcji (autor sam to komentuje jako świadomy kompromis) | Ta sama klasa problemu co W4 | Jak W4 | Niski-średni |
| W6 | `getBasenAdminGodzinyUsersHandler.ts:109-111`, `getKayakReservationsHandler.ts:106-108,125-128`, `getKursantStatsHandler.ts:75-77`, `getAdminPendingHandler.ts:216-224` | `Promise.all(ids.map(id => doc.get()))` zamiast jednego `db.getAll(...refs)` | Poprawny wzorzec **już istnieje** w tym samym repo (`getAdminGearRentalsHandler.ts:215`, `getAdminGearTopRentalsHandler.ts:240`, `getAdminMemberActivityHandler.ts:156`) — niespójne stosowanie | Zamienić na `db.getAll(...refs)` w 4 plikach / 5 miejscach | Średni |
| W7 | `submitEventHandler.ts:88` | Dedup 60s pobiera **całą** historię imprez usera zamiast ostatnich N | Niepotrzebny odczyt rosnący z historią zgłoszeń | Dodać `.orderBy("createdAt","desc").limit(20)` | Niski |

### 2.2 Poprawność

| # | Plik:linia | Problem | Dlaczego to problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|---|
| P1 | `getKayakReservationsHandler.ts:76-80,87-91` | `status==` + `itemIds`/`kayakIds` `array-contains` bez odpowiadającego indeksu w `firestore.indexes.json` | Zob. sekcja 4.1 — ocena ryzyka rozbieżna między agentami audytu, **do zweryfikowania w konsoli Firebase**, nie pewny fakt | Dodać indeksy prewencyjnie (koszt: 2 wpisy w JSON) LUB zweryfikować w logach produkcyjnych brak `FAILED_PRECONDITION` | Wysoki (do weryfikacji) |
| P2 | 11 plików (`gearMyReservationsHandler.ts`, `gearReservationCancelHandler.ts`, `gearReservationCreateHandler.ts`, `gearReservationUpdateHandler.ts`, `getBasenAttendeesHandler.ts`, `getBasenKayaksHandler.ts`, `getBasenSessionsHandler.ts`, `getGodzinkiHandler.ts`, `godzinkiPurchaseHandler.ts`, `registerUserHandler.ts`, `submitGodzinkiHandler.ts`) | Brak guardu `if (req.method !== "...") return res.status(405)...` | 48/59 handlerów ma tę walidację, te 11 nie — w tym endpointy mutujące dane. Nie luka bezpieczeństwa (auth nadal wymagany), ale niespójność wejścia | Dodać identyczny 3-liniowy guard jak w pozostałych 48 plikach. Mechaniczne, 11 plików | Średni |
| P3 | `gearReservationCreateHandler.ts:55-61` | `logger.info` logujący pełną zawartość dokumentu usera przy każdej próbie rezerwacji | Nadmiarowy log diagnostyczny w produkcji, ujawnia strukturę danych usera w logach | Usunąć albo `logger.debug`. 1 plik | Niski |
| P4 | 8 plików basen (`basenEnrollHandler.ts` i 7 pokrewnych) | Klasyfikacja 400 vs 500 przez `msg.includes("nie istnieje")` itp. — ręcznie utrzymywana lista substringów | Literówka/zmiana treści komunikatu w `basen_service.ts` cicho przełącza kod odpowiedzi bez ostrzeżenia kompilatora | Klasa `ClientError extends Error {status}` rzucana w `basen_service.ts`, odczytywana jednolicie. Średni zakres — `basen_service.ts` + 8 handlerów | Niski-średni |

### 2.3 Duplikacja

| # | Pliki | Problem | Ile razy | Zakres naprawy | Priorytet |
|---|---|---|---|---|---|
| D1 | 22 pliki w `api/` | Lokalna `function norm(v){return String(v||"").trim();}` kopiowana zamiast importu ze wspólnego `modules/shared/text_utils.ts` (który **już istnieje** z komentarzem „D4: norm() było kopiowane per plik” — problem już raz „zamknięty”, regresja wróciła) | 22× | Podmienić import w 21 plikach (1 już poprawny), usunąć lokalne definicje | Średni |
| D2 | `getAdminGearRentalsHandler.ts`, `getAdminGearTopRentalsHandler.ts`, `getAdminMemberActivityHandler.ts`, `getAdminUserActivityHandler.ts` | Identyczne `todayWarsawIso()`, `isoToDateUTC()`, `dateUTCToIso()`, `minusDays()`, `minusMonths()`, `isIsoDate()` + prawie identyczna `resolveRange()` z **różnymi** definicjami „miesiąca” (`minusDays(30)` vs `minusMonths(1)`) | 4× | Wydzielić `modules/shared/date_range_utils.ts` z jedną parametryzowalną `resolveDateRange()`. 4 pliki | Średni |
| D3 | `getBasenAdminGodzinyUsersHandler.ts` + 5 innych plików raportowych | `fullName(u)`/`nickname(u)` kopiowane identycznie | 6× | Wydzielić `modules/shared/user_display.ts`. 6 plików | Niski-średni |
| D4 | `getAdminGearRentalsHandler.ts:22-29`, `getAdminGearTopRentalsHandler.ts:23-30` | Stała `CATEGORY_LABELS` zduplikowana 1:1 | 2× | Wydzielić do `gear_catalog_service.ts` (już importowany w obu plikach) | Niski |
| D5 | `gearFavoriteToggleHandler.ts:20-22`, `getGearFavoritesHandler.ts:19-21` | Stała `VALID_CATEGORIES` zduplikowana 1:1 | 2× | Wydzielić do wspólnego modułu sprzętowego | Niski |
| D6 | `index.ts` (3× inline) + `adminApprovalHandler.ts:29-41`, `adminGearReservationCancelHandler.ts:24-35`, `submitGearDamageReportHandler.ts:11-22`, `adminEventsSyncCalendarHandler.ts:57-69`, `kmAdminMergePlacesHandler.ts:196-205` | Boilerplate tworzenia dokumentu `service_jobs` przepisywany niezależnie zamiast jednej funkcji `enqueueServiceJob(db, taskId, payload)` | ~9× | Wydzielić do `service/job_queue.ts`, podmienić 9 miejsc | Niski-średni |
| D7 | `getAdminUserActivityHandler.ts`, `getAdminMemberDuesHandler.ts`, `getBasenAdminGodzinyUsersHandler.ts` | `isRegistered(u)` zduplikowana identycznie | 3× | Wydzielić razem z D3 | Niski |

### 2.4 Martwy kod

| # | Plik | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| M1 | `functions/src/api/gearKayaksListHandler.ts` (cały plik) | Eksportuje `handleGearKayaksList` — 0 importów w repo, brak wpisu w `index.ts`/`firebase.json` → endpoint całkowicie nieosiągalny | Usunąć plik; sprawdzić przy okazji czy `listKayaks` z `gear_kayaks_service.ts` ma innych konsumentów (jeśli nie — usunąć też) | Niski |

### 2.5 Niespójności strukturalne

| # | Pliki | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| N1 | Rodzina `admin/reports/*` (5 handlerów) | Każdy raport implementuje własną wersję rozwiązywania zakresu dat i mapowania nazw — mimo wspólnego kształtu (`?range=&from=&to=` → wiersze z `userName/userNick/userEmail`) | Wspólny szkielet `runReport(req,res,deps,{resolveRows})` + wspólny `resolveDateRange` (łączy się z D2). Duży redesign 5 plików, wysoka wartość porządkowa | Średni |
| N2 | Patrz W6 | 3 pliki poprawnie używają `db.getAll()`, 4 pliki/5 miejsc gorszego wzorca dla identycznego zadania | Patrz W6 | Średni |
| N3 | `gearReservationCreateHandler.ts`/`UpdateHandler.ts`/`CancelHandler.ts` (brak walidacji metody) vs `gearBundleReservationCreateHandler.ts`/`UpdateItemsHandler.ts` (walidacja po `requireIdToken`, nie przed, jak w pozostałych 48) | Ta sama rodzina „rezerwacje sprzętu” ma 3 różne warianty kolejności/obecności walidacji | Naprawić razem z P2, ujednolicić kolejność (metoda → token) | Niski |

---

## 3. Backend — logika biznesowa i system zadań asynchronicznych (`functions/src/modules/**` + `functions/src/service/**`, 71 plików)

### 3.1 System jobów (`service/worker/*`, `service/registry.ts`)

| # | Plik:linia | Problem | Dlaczego to problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|---|
| J1 | `service/worker/jobProcessor.ts:29-46` + `service/worker/fallbackDailyWorker.ts:18-24` | Pole `lockedUntil` jest martwe dla swojego celu: `if (data.status !== "queued") return false` blokuje reclaim zawsze, gdy `status=="running"`, niezależnie czy lock wygasł. `serviceFallbackDaily` odpytuje wyłącznie `status=="queued"` | Job przerwany w trakcie (timeout, restart instancji) **po** ustawieniu `running`, **przed** finalnym statusem — zostaje w `"running"` na zawsze. Ani trigger, ani fallback go nie widzą. Efekt: cichy, trwały brak wykonania, zero retry, zero alertu | Dodać w `fallbackDailyWorker.ts` (lub osobnym schedulerze) zapytanie `status=="running" AND lockedUntil<now`, resetujące do `"queued"`. Średnia złożoność, 1-2 pliki. **Zalecenie natychmiastowe: sprawdzić ręcznie w Firestore, czy istnieją dziś dokumenty `service_jobs` ze `status=="running"` starsze niż kilka godzin** | **Wysoki** |
| J2 | `service/tasks/basenNotifySessionCancelled.ts:83-87`, `service/tasks/gearNotifyReservationCancelledByAdmin.ts:178-179`, `service/tasks/usersSyncFunctionRolesFromSetup.ts:311-521` | Brak idempotencji per-odbiorca przy wysyłce maili — kontrastuje z poprawnym wzorcem `remindersSentToUids` w `eventsNotifyUpcoming.ts:129,180` | Retry całego tasku po jednym nieudanym mailu → ponowna wysyłka do **wszystkich** odbiorców, łącznie z tymi którzy już dostali. Realny scenariusz: uczestnik anulowanego basenu dostaje 2-3 identyczne maile | Dodać znacznik „wysłano do X” analogicznie do `remindersSentToUids`; w `usersSyncFunctionRolesFromSetup` zapisywać stan po każdej roli, nie zbiorczo na końcu. 3 pliki | **Wysoki** |

*(Kontrola bez zastrzeżeń: retry/backoff `[30,60,120,300,900]`s i przejście do `dead` po `maxAttempts` działają poprawnie i spójnie — poza luką J1. Rejestr `service/registry.ts` — wszystkie 36 tasków zarejestrowane 1:1, brak sierot.)*

### 3.2 Wydajność

| # | Plik:linia | Problem | Dlaczego to problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|---|
| P1 | `service/providers/googleAuth.ts:64-134` (`getDelegatedAuth`) | Brak cache tokena/klienta OAuth — każde wywołanie `getDirectoryClient`/`getGmailClient`/`getGroupsSettingsClient`/`getSheetsClient`/`getCalendarClient` robi pełną wymianę (IAM signJwt + token exchange = 2 round-tripy) mimo że token jest ważny 1h | Dotyka niemal wszystkich tasków z >1 operacją: `eventsNotifyUpcoming.ts` (mail/uid), `usersSyncRolesFromSheet.ts`/`usersSyncFieldsFromSheet.ts` (per wiersz arkusza), `usersSyncFunctionRolesFromSetup.ts` (do 8 maili), `eventsSyncFromSheet.ts` (per wiersz + Calendar). Zwiększa czas wykonania i ryzyko timeoutu (P7) oraz zawieszenia jobu (J1) | Cache token per `(delegatedUserEmail, scopes)` w `getDelegatedAuth` (Map z TTL ~50 min). 1 plik, efekt szeroki | **Wysoki** |
| P2 | `modules/equipment/bundle/gear_bundle_service.ts:438-481,487-519`, `modules/equipment/shared/reservation_limits.ts:26-63` | Zapytania konfliktów filtrują tylko `status=="active"` — potwierdzone grepem: **żaden** task/handler nigdy nie zmienia statusu na `completed`/`expired`. Jedyne przejście poza `active` to anulowanie | Każda nowa rezerwacja/edycja (w transakcji!) skanuje **całą historyczną pulę** aktywnych rezerwacji klubu od początku istnienia modułu. Koszt rośnie liniowo i bezpowrotnie z każdym rokiem, zwiększa ryzyko konfliktów/retry transakcji Firestore | Mechanizm (trigger/scheduler) przełączający `active→completed` po `endDate`, filtrowanie po `blockEndIso>=today` u źródła. Średnia-duża złożoność (migracja + 3-4 pliki + ew. indeks) | **Wysoki** |
| P3 | `gear_bundle_service.ts:308-340` (`fetchItemDetails`) | Pełny odczyt (`.where("isActive","==",true).get()`, bez limitu) całej kolekcji kategorii sprzętu przy każdej rezerwacji/edycji, mimo że potrzeba tylko kilku konkretnych `itemId`. Dla porównania `getItemsWithAvailability` (ten sam plik) i `gear_catalog_service.ts:122-126` mają `.limit(500)` | Koszt rośnie z rozmiarem inwentarza mimo że wynik i tak filtrowany do żądanych ID | Zamienić na `db.getAll(...refs)` po konkretnych `doc(itemId)`. 1 plik | Średni |
| P4 | `modules/hours/godzinki_service.ts:147-188` (`computeNegativeBalances`) | Pełny skan całej kolekcji `godzinki_ledger` bez limitu, grupowanie w pamięci. Wołane przy każdym wejściu admina w panel oraz co miesiąc przez `godzinkiMonthlyBalanceReview.ts:110` | Koszt rośnie z całą historią ledgera klubu; ścieżka interaktywna (panel) płaci ten koszt za każdym odświeżeniem | Zdenormalizowane bieżące saldo per-user aktualizowane przy każdej transakcji ledgera. Duża złożoność (zmiana modelu danych) — do rozważenia długoterminowo | Średni |
| P5 | `service/tasks/usersSyncRolesFromSheet.ts:153-157`, `usersSyncFieldsFromSheet.ts:387` | N+1: osobne zapytanie Firestore per wiersz arkusza członków, sekwencyjnie w `for...await` | Dla ~100-300 członków to 100-300 kolejnych round-tripów; ryzyko timeoutu (P7) i zawieszenia jobu (J1) | Zbudować mapę `email→doc`/`memberId→doc` jednym zapytaniem zbiorczym przed pętlą (`users_active` mieści się w 1 `.get()`). 2 pliki | Średni |
| P6 | `service/tasks/eventsSyncFromSheet.ts:335,368,383,410-417` | Sekwencyjny odczyt+zapis Firestore, zapis do arkusza i wywołanie Calendar API — wszystko per wiersz imprezy w jednej pętli | Czas wykonania rośnie szybciej niż liczba wierszy (Firestore + Sheets + Calendar + auth × N). Niski wolumen dziś, ale kosztowny wzorzec | Ograniczyć do faktycznie zmienionych wierszy lub grupować w `batchUpdate`. Średnia złożoność | Niski-średni |
| P7 | `service/admin/adminRunTask.ts:15` | `timeoutSeconds: 30` dla synchronicznego wywołania z panelu admina — za mało dla dużych synców w połączeniu z P1/P5 | Ręczne uruchomienie dużego syncu z panelu może zostać ucięte mid-task, zostawiając częściowy stan i przyczyniając się do J1 | Podnieść timeout (300-540s) LUB przełączyć ręczne triggery dużych tasków na kolejkowanie przez `service_jobs`. 1 plik | Średni-wysoki |
| P8 | `service/worker/onJobCreatedWorker.ts:4-19` | `onDocumentWritten` odpala się na każdy zapis do `service_jobs/{jobId}`, także te wykonane przez sam `jobProcessor` (poprawnie no-opują, ale kosztują) | Niepotrzebny narzut wywołań/kosztu Cloud Functions rosnący z liczbą jobów | Rozważyć `onDocumentCreated` dla ścieżki natychmiastowej. Mała złożoność, czysto kosztowa zmiana | Niski |

### 3.3 Duplikacja

| # | Pliki | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| D1 | `modules/shared/text_utils.ts::norm` istnieje, ale **~35 lokalnych redefinicji** w `gear_bundle_service.ts`, `reservation_limits.ts`, `gear_kayaks_service.ts`, `gear_catalog_service.ts`, `gear_damage_service.ts`, `events_service.ts`, `basen_service.ts` (nawet eksportowana jako publiczna kopia!), `workspaceGroupSync.ts`, i ~15 tasków `service/tasks/*` | Ryzyko rozjazdu implementacji — projekt już raz próbował to scentralizować (komentarz „D4”), regresja wróciła | Podmienić lokalne definicje na import w ~25 plikach. Mechaniczne, czasochłonne | Niski-średni |
| D2 | `modules/setup/app_vars.ts`, `modules/km/km_vars.ts`, `modules/hours/godzinki_vars.ts`, `modules/setup/events_vars.ts`, `modules/setup/setup_gear_vars.ts` | Identyczny wzorzec `getVar()`+`toNumber()`/`toBool()`/`toStr()` redefiniowany od zera w 5 plikach `*_vars.ts` | Zmiana reguły parsowania wymaga edycji w 5 miejscach | Wydzielić `getVarValue`/`toNumberSafe`/`toBoolSafe` do `modules/shared/vars_utils.ts`. 5 plików | Niski |
| D3 | `service/tasks/gearSyncAllFromSheet.ts:479-491` vs `:368` | Każda z 9 zakładek arkusza sprzętu czytana **dwa razy** w jednym przebiegu (raz w walidacji, raz w `syncCategory`) | Podwaja liczbę odczytów Google Sheets API (9→18) bez potrzeby | Przekazać już wczytane tabele do `syncCategory` zamiast odczytywać ponownie. 1 plik | Niski-średni |

### 3.4 Martwy kod

| # | Plik:linia | Problem | Weryfikacja | Priorytet |
|---|---|---|---|---|
| M1 | `modules/hours/godzinki_service.ts:987` (`creditReservationAdjustment`) | Eksportowana, 0 wywołań w repo. Sama oznaczona w komentarzu jako „LEGACY” | grep — tylko definicja | Niski |
| M2 | `modules/hours/godzinki_service.ts:770` (`refundHoursForReservation`, wrapper non-tx) | 0 wywołań — używana wyłącznie wersja transakcyjna `refundHoursForReservationInTx` | grep — tylko definicja | Niski |
| M3 | `service/providers/googleWorkspaceProvider.ts:60` (`listMembersOfGroup`) | 0 wywołań — wszędzie używana `listMembersDetailed` | grep — tylko definicja | Niski |
| M4 | `service/providers/googleSheetsProvider.ts:561-566` (`upsertMemberRowByEmail`) | Ciało to wyłącznie `throw new Error("...deprecated...")`, 0 wywołań | grep — tylko definicja | Niski |

### 3.5 Poprawność

| # | Plik:linia | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| K1 | `test/gear_core.test.ts` | Przestarzały test (patrz sekcja 0) | Zaktualizować test: oczekiwać realnego kosztu + `waived:true`, nie `0` | Niski (kosmetyczny) |
| K2 | `service/tasks/kursSyncFromSheet.ts:27` | Jedyny task bez konwencji `domena.metoda` w `id` (wszystkie pozostałe 35 mają wzorzec `"gear.syncAllFromSheet"` itd.) | Zmiana `id` na `"kurs.syncFromSheet"` — **breaking change dla ew. zaplanowanych jobów w kolejce**, wymaga koordynacji z deployem | Niski |

---

## 4. Warstwa danych — indeksy Firestore, zależności, higiena repo, pokrycie testami

### 4.1 Indeksy Firestore vs realne zapytania

`firestore.indexes.json` = 16 composite indexes. Zgrepowano 174 wystąpienia `.where`/`.orderBy` w `functions/src`.

| # | Kolekcja | Pola zapytania | Plik:linia | Indeks? | Pewność |
|---|---|---|---|---|---|
| 1 | `service_jobs` | `status==` + `nextRunAt<=` + `orderBy(nextRunAt)` | `service/worker/fallbackDailyWorker.ts:18-24` | **Brak** — `service_jobs` w ogóle nie ma wpisu w pliku indeksów | **Wysoka** (równość+zakres+sort na innym polu = bezsporny wymóg composite index) |
| 2 | `gear_reservations` | `userUid==` + `orderBy(createdAt,desc)` | `gear_bundle_service.ts:1339-1345` (`listMyBundleReservations`) | **Brak** — 2 istniejące indeksy tej kolekcji mają inny zestaw pól | **Wysoka** — dotyczy każdego użytkownika (moduł „moje rezerwacje” auto-wstrzykiwany) |
| 3-4 | `gear_reservations` | `status==` + `itemIds`/`kayakIds` `array-contains` | `getKayakReservationsHandler.ts:76-91` | Brak | **Niska/średnia** — per dokumentacja Firestore taki wzorzec bywa zwolniony z composite index; niezweryfikowane w konsoli produkcyjnej |
| 5-7 | `basen_enrollments` | `sessionId==`+`userUid==`+`status==` / `sessionId==`+`slot==`+`instructorUid==`+`status==` / `+type==` | `basen_service.ts:443-458,711-715,1047-1051,1079-1083` | Brak (istniejące 3 indeksy mają inny zestaw pól) | Niska — równość-tylko, ale **w transakcji rezerwacji basenu** — warto zweryfikować w logach |
| — | `km_logs` | indeksy `(uid+year+km desc)` i `(year+pointsTotal desc)` istnieją, ale **brak jakiegokolwiek zapytania** w kodzie które by ich używało | — | Odwrotny problem — potencjalnie martwe indeksy (koszt utrzymania przy zapisie, nie funkcjonalny bug) | Niski |

**Potwierdzone jako poprawnie pokryte** (nie wymagają akcji): `events`(approved+endDate/createdAt/startDate, kierownikUids CONTAINS+approved+endDate), `godzinki_ledger`(approved+type+createdAt, uid+createdAt desc), `gear_reservations`(status+userUid+blockStartIso, status+blockStartIso), `km_logs`(uid+date desc), `km_places`(searchTerms CONTAINS+useCount desc).

### 4.2 Higiena repo / martwe artefakty

| Co | Plik(i) | Potwierdzenie | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| Stary raport audytu | `ai_full_audit_report.txt` (89 KB) + `.json` (507 KB), **trackowane w git** | Generator `project_optimization_snapshot.py` nie istnieje w repo; gałąź `dev-reset-from-prod` nie istnieje | `git rm` obu plików | Niski |
| Zdublowany katalog kontekstu | `.claude_context/` (14 MB, 9 plików, **trackowany w git**, ostatnio zmieniony w bardzo starym commicie) vs aktywny `_claude_context/` (1,9 MB, świeżo modyfikowany) | Dwa równoległe narzędzia: `tools/build_project_context.py` (pisze do martwego `.claude_context/`, brak referencji w kodzie/CI) vs `project_context.py` w korzeniu (pisze do aktywnego `_claude_context/`) | `git rm -r .claude_context/` + usunięcie `tools/build_project_context.py` | Średni (rozmiar w git) |
| `archived/functions/node_modules` | 58 MB na dysku | Nietrackowane (łapie się pod `.gitignore` dla `node_modules`), brak referencji w `firebase.json`/`index.ts`, poza `node_modules` katalog jest pusty | Bezpieczne usunięcie z dysku — zero wpływu na git | Niski |
| `.venv`, `.pytest_cache` w korzeniu | — | Nietrackowane dzięki własnym wewnętrznym `.gitignore`, ale nie przez root `.gitignore` — kruche | Dodać `.venv/` i `.pytest_cache/` wprost do root `.gitignore` | Niski |

*(Kontrola bez zastrzeżeń: `functions/lib` poprawnie w `.gitignore`, 0 plików trackowanych.)*

### 4.3 Zależności (`functions/package.json`)

| Pakiet | Obecna | Najnowsza | Uwaga |
|---|---|---|---|
| `firebase-admin` | 13.6.1 | 14.3.0 | 1 major w tyle |
| `googleapis` | 144.0.0 | 178.1.1 | 34 wersje w tyle |
| `@typescript-eslint/*` | 5.62.0 | 8.70.0 | 3 major w tyle |
| `eslint` | 8.57.1 | 10.10.0 | 2 major w tyle |
| `typescript` | 5.9.3 | 7.0.2 | 2 major w tyle |
| `@google-cloud/secret-manager` | 5.6.0 | 7.1.0 | **0 importów w `functions/src` — martwa zależność produkcyjna**, wchodzi do bundla deployu |

| Problem | Zakres naprawy | Priorytet |
|---|---|---|
| `@google-cloud/secret-manager` nieużywana (produkcyjna) | `npm --prefix functions uninstall @google-cloud/secret-manager` + weryfikacja buildu | Średni (łatwe, zero ryzyka) |
| `firebase-functions-test` nieużywana (dev) | Można usunąć — zero importów `.ts` | Niski |
| eslint 8 + `@typescript-eslint` 5 (2022) vs `typescript` 5.9 (2026) — niedopasowane deklarowane wsparcie | Upgrade do eslint 9/10 + `@typescript-eslint` 8.x — wymaga migracji `.eslintrc.js` → flat config `eslint.config.js` (breaking change formatu). Nie pilne | Niski-średni |

### 4.4 Pokrycie testami

Z **62 handlerów** `api/` — **61 (98%) bez bezpośredniego testu jednostkowego** (1 częściowo). Z **34 tasków** `service/tasks/` — **27 (79%) bez testu**.

| Obszar biznesowy | Pokrycie |
|---|---|
| **Basen** (15 handlerów + 2 taski, w tym `basen_service.ts`/`basen_godziny_service.ts`) | **0%** |
| **KM/kilometrówka** (8 handlerów + 4 taski, `km_log_service.ts`/`km_places_service.ts`/`km_scoring.ts`) | **0%** |
| **Klub** (`getKlubInfoHandler.ts`) | **0%** |
| **Sprzęt — rdzeń rezerwacji/limitów/konfliktów** (`gear_bundle_service.ts`, `reservation_limits.ts`, `gear_kayaks_service.ts`, 8 handlerów) | **0%** — mimo udokumentowanej w pamięci projektu historii wielu bugfixów w tym obszarze |
| **Sprzęt — uszkodzenia** (3 handlery + 1 task) | **0%** |
| **Raporty administracyjne** (6 handlerów) | **0%** |
| **Rejestracja/bootstrap ról** (`registerUserHandler.ts` + 5 tasków sync) | **0%** — krytyczna ścieżka bez testu |
| **Wydarzenia** | Częściowe — rdzeń (`events_service.ts`) testowany, warstwa HTTP/Calendar nie |
| **Godzinki** | Częściowe — rdzeń bilansu testowany, warstwa handlerów/część tasków nie |
| **Workspace/infra sync** (8 tasków) | Częściowe — tylko `workspaceGroupSync.ts` testowany |

Testowane pliki dziś (7 plików test/*): `workspaceGroupSync.ts`, `hours_quote.ts`, `setup_gear_vars.ts`, `calendar_utils.ts`, `gearPrivateStorage.ts`, `gearSyncAllFromSheet.ts`, `godzinkiSyncFromSheet.ts`, `adminNotifyPendingApprovals.ts`, `godzinkiArchiveSheetRows.ts`, `eventsSyncFromSheet.ts`, `googleSheetsProvider.ts`, `eventsNotifyUpcoming.ts`, `events_service.ts`, `godzinki_service.ts`.

---

## 5. Frontend (`public/core/**`, `public/modules/**`, `public/styles/**`, `sw.js`, 44 pliki)

### 5.1 Martwy kod

| Plik:linia | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|
| `styles/gear.css:197-212,430-445` + `gear_module.js:1706-1708,1747` | `.gearInlineMeta*`/`.gearCardActions` — `display:none` bazowo, ale nadal wstawiane w HTML (autor już to wie, komentarz cytuje wcześniejsze ustalenie `project_gear_card_dead_layout`) | Usunąć 4 linie HTML + ~20 linii CSS. 2 pliki | Niski |
| `styles/gear.css:187-195,689-728,730-738,1195-1230` | `.gearInlineMetaWrap/Stack`, `.gearModalThumbs/.gearThumbBtn` (stary modal miniaturek), `.gearStatusLine`, `.gearReservSectionTitle/ModalSection/ModalTitle` — brak referencji w JS | Usunąć ~100 linii CSS łącznie. 1 plik | Niski |
| `styles/dashboard.css:49-161` | Cała stara karta-siatka dashboardu (~113 linii, 70% pliku) zastąpiona wzorcem `.startList*` w `render_shell.js` | Usunąć blok | Niski |
| `styles/kurs.css:6-30,355-425` | `.kursTabs/.kursTab` (stara nawigacja) + `.kursInfoCard/.kursEventsSection*` (usunięta zakładka „Harmonogram”) | Usunąć ~95 linii | Niski |
| `styles/start.css:19-118,145-157,186-190,266` | Stary układ 2-kolumnowy kafelków zastąpiony `.startTile2`/`.startTileGrid` | Usunąć ~115 linii | Niski |
| `styles/godzinki.css:123-127`, `styles/basen.css:49-91` | `.godzinkiMeta`, `.basenCardMeta/.basenCardFooter` — brak referencji | Usunąć | Niski |
| `core/render_shell.js:1673-1691` | `getModuleRouteByLabelOrId()` zdefiniowana, 0 wywołań w całym `public/` | Usunąć funkcję (~20 linii) | Niski |
| `sw.js:21-55` (`PRECACHE_URLS`) | Brak 12 aktualnych plików: `date_range_calendar.js`, `club_badges.js`, `klub_module.js`, `kurs_module.js`, `kurs_godzinki_module.js`, cały `modules/raporty/*` (8 plików), `godzinki.css`, `kurs.css`. `bump-sw-cache.js` podbija tylko wersję, nie synchronizuje listy | Pierwsze offline-gotowe wejście/instalacja PWA nie ma tych plików w cache (nie krytyczne — fetch handler i tak cache'uje przy realnym użyciu, ale przeczy deklarowanemu celowi). Uzupełnić listę o 12 plików, rozważyć generowanie automatyczne w `bump-sw-cache.js` | Średni |

### 5.2 Duplikacja

| Miejsca | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|
| 15+ plików (`render_shell.js`, `club_badges.js`, `text_format.js`, `gear_module.js`, `my_reservations_module.js`, `admin_pending_module.js`, `klub_module.js`, 4× `esc()` w modułach, 6× w `raporty/*`) | `escapeHtml`/`escapeAttr` reimplementowane niezależnie zamiast wspólnego importu | Wydzielić `core/html_utils.js`, podmienić ~20 plików | Średni |
| 7 plików (`render_shell.js`, `gear_module.js`, `my_reservations_module.js`, `admin_pending_module.js`, `date_range_calendar.js`, `raporty/gear_rentals.js`, `raporty/gear_damage_reports.js`) | `formatDatePL` identyczna w 7 miejscach | Dołączyć do `core/format_utils.js` razem z `escapeHtml`. Mała/średnia | Średni |
| `render_shell.js:1379`, `gear_module.js:2067`, `my_reservations_module.js:637` | `buildKayakTitle(k)` bajtowo identyczna w 3 plikach | Przenieść do core, 3 importy | Średni |
| `render_shell.js:1821-1838`, `my_reservations_module.js:647-664` | `formatShortDate`/`countReservationDays`/`pluralizeDays` identyczne | Dołączyć do wspólnego modułu formatującego | Niski |
| `render_shell.js:398-414`, `klub_module.js:36-47`, `km_module.js:34-45`, `map.html:158-163` | `openMap()` identyczna w 4 miejscach | Wydzielić do `core/`, 3 importy JS | Niski |
| `render_shell.js:809-849` vs `klub_module.js:159-182` | Logika „kopiuj do schowka + fallback” duplikowana niemal 1:1 | Wydzielić wspólną funkcję | Niski |
| `gear_module.js:1660-2170` (5 funkcji renderujących karty: kajak/kask/wiosło/kamizelka/generyczna) | Powielają identyczny szkielet (`gearHead`/`gearTitleWrap`/`gearMiniBar`/`gearBadges`), różnią się tylko mapowaniem pól. Widoczne ślady niespójności (`.gearBadges` wstawiane w 3/5 kart) | Wydzielić `renderGearCardShell({header,media,footer,badges})`, sparametryzować 5 kart. **Duża złożoność** (najbardziej wrażliwy plik, 148 KB) — robić stopniowo z testami wizualnymi | Średni |
| `render_shell.js:961-1008` vs `impreza_module.js:70-115` | Karta imprezy renderowana niezależnie w 2 miejscach z przypadkowymi różnicami (np. truncacja opisu tylko w jednym) | Wydzielić wspólną funkcję (parametr skrócony/pełny) — musi trafić do `core/`, bo `render_shell.js` (core) nie może importować z `modules/` | Średni |
| `godzinki_module.js:94-160` vs `raporty/user_activity.js:9-52` | Jawnie skopiowane (komentarz w kodzie: „Helpery skopiowane z godzinki_module.js”) | Wyeksportować z `godzinki_module.js` (lub wspólnego modułu), zaimportować w raporcie | Średni |
| `raporty/gear_rentals.js`, `top_rentals.js`, `member_activity.js`, `user_activity.js` | Selektor zakresu + grupa checkboxów kategorii niemal identyczny w 3-4 plikach | Wydzielić `raporty/report_controls.js`. Wysoka wartość przy skalowaniu liczby raportów | Średni |
| `styles/gear.css:541-720,866-925` vs `styles/basen.css:359-434` | Cały system modala zduplikowany pod różnymi prefiksami — komentarz w kodzie przyznaje że to celowe (uniknięcie zależności `basen_module.js`→`gear.css`), ale uzasadnienie **nieaktualne**: `app.css` i tak ładuje oba pliki zawsze (patrz 5.3) | Usunąć `.basenModal*`, podmienić na `.gearModal*` albo przenieść wspólne reguły do `base.css` pod neutralną nazwą. 2 pliki CSS + ~15 miejsc w `basen_module.js` | Średni |
| `styles/gear.css:1582-1822` (`.gearCal*`) vs `basen_module.js`+`render_shell.js` (`.basenCal*`, 2× zaimplementowane od zera) | Dwa równoległe systemy kalendarza-siatki zamiast reużycia jednego generycznego komponentu (wzorem rezerwacji sprzętu) | Refaktor architektoniczny — do rozbicia na osobne zadania | Niski |

### 5.3 Wydajność

| Plik:linia | Problem | Dlaczego to problem | Zakres naprawy | Priorytet |
|---|---|---|---|---|
| `index.html:22` + `styles/app.css:1-9` | `app.css` przez `@import` ściąga wszystkie 9 arkuszy (~113 KB) na każdym ekranie, dla każdej roli — niezależnie od tego, do których modułów user ma dostęp. `@import` w CSS blokuje renderowanie sekwencyjnie (przeglądarka musi pobrać+sparsować `app.css` zanim odkryje kolejne importy), gorzej niż równoległe `<link>` | Wolniejszy pierwszy render, szczególnie na słabym/mobilnym łączu; zbędny transfer dla ról bez dostępu do części modułów | **Krótkoterminowo:** zamienić 9 `@import` na 9 równoległych `<link rel="stylesheet">` w `index.html`. 2 pliki, natychmiastowa poprawa. **Docelowo:** ładowanie CSS modułu dynamicznie przy pierwszym wejściu | **Wysoki** |
| Wszystkie moduły (`gear_module.js`, `godzinki_module.js`, `basen_module.js`, `km_module.js`, `impreza_module.js`, `klub_module.js`, `admin_pending_module.js`) | `render()` zawsze odpytuje API od nowa, brak cache w `ctx` — nawigacja Start→Sprzęt→Start→Sprzęt pobiera pełną listę ponownie | Niepotrzebny ruch sieciowy i spinner przy każdym powrocie do zakładki | Prosty cache w pamięci per moduł (`Map` w `ctx` z TTL kilku minut) dla danych tylko-do-odczytu. Zacząć od modułu Sprzęt (największy ruch) | Średni |
| `core/api_client.js:54-94` | Brak `AbortController` — żaden `fetch()` nie jest anulowany przy zmianie widoku w trakcie oczekiwania | Przy szybkim przeklikiwaniu modułów kilka zapytań kontynuuje się w tle jednocześnie (marnotrawstwo transferu/baterii; dziś nieszkodliwe wizualnie, bo `innerHTML` i tak nadpisywany) | `AbortController` tworzony przy starcie `renderView()`, przekazywany do `mod.render()`, `abort()` przy kolejnym wywołaniu. Dotyka `api_client.js` + `render_shell.js` + sygnaturę `render()` wszystkich modułów | Niski |
| `basen_module.js:353-552` (`bindSessionActions`) | ~8 osobnych bloków `querySelectorAll().forEach(addEventListener)` tworzonych od nowa przy **każdym** `renderSessionsView`/`refreshSessionsView()`. Ten sam plik ma już wzorzec delegacji (`bindModalCloseDelegation:554`) wprowadzony po realnym incydencie spowolnienia (komentarz `:1499-1506`), ale nie zastosowano go do reszty akcji | Przy dużej liczbie dni×slotów każde odświeżenie tworzy dziesiątki-setki nowych listenerów zamiast 1 delegowanego | Zastąpić 8 bloków jedną delegacją zdarzeń na `innerEl`, analogicznie do `bindModalCloseDelegation`. ~150 linii, 1 plik | Średni |
| `firebase_client.js`/`gear_module.js:22` (`PLACEHOLDER_SVG`) | `assets/kayak-placeholder.png` waży **1,4 MB** jako placeholder KAŻDEJ karty sprzętu przed załadowaniem prawdziwego zdjęcia. Nazwa stałej sugeruje mylnie lekki SVG | 100-500× cięższy niż potrzeba typowy placeholder — spowalnia pierwsze wejście do listy sprzętu na wolnym łączu | Podmienić na lekki plik (SVG lub PNG <20 KB). 1 plik graficzny | **Wysoki (łatwe, wysoki zwrot)** |

### 5.4 Poprawność

| Plik:linia | Problem | Zakres naprawy | Priorytet |
|---|---|---|---|
| `styles/gear.css:214-219` + `gear_module.js:1719,1840,1928,1976` | `.gearBadges` ma `display:none` jako regułę BAZOWĄ (nie tylko w `@media max-width:600px`, gdzie jest powtórzona) — plakietka „Basen” i inne **nigdy się nie renderują**, ani mobile, ani desktop. Komentarz w kodzie sugeruje, że autor sądził inaczej — wygląda na niezamierzoną regresję, nie świadomą decyzję (w odróżnieniu od `.gearInlineMeta`) | Decyzja produktowa: przywrócić widoczność (usunąć bazowe `display:none`) albo świadomie usunąć jako martwe. Mała zmiana CSS | **Wysoki** |
| `km_module.js` (6 miejsc: `renderFormView:812`, `renderRankingsView:918`, `renderMyStatsView:958`, `renderMyLogsView:1066`, `renderEventStatsView:1129`, `renderKursantRankingView:1243`) | Błędy API pokazywane jako surowe `err?.message`, zamiast `mapUserFacingApiError()` z `user_error_messages.js`, którego konsekwentnie używają pozostałe moduły | Użytkownik widzi techniczny komunikat zamiast czytelnego PL. Zamienić w 6 miejscach, 1 plik | Średni |
| `render_shell.js:844-848` (`wireKlubBox`) | Błąd API pokazuje generyczny tekst bez `mapUserFacingApiError`, w odróżnieniu od `klub_module.js:274-278` | Drobna niespójność UX błędów dla tej samej domeny | Ujednolicić na `mapUserFacingApiError`. 1 plik | Niski |

### 5.5 PWA / Service Worker

| Co | Ocena | Priorytet |
|---|---|---|
| `sw.js:99-156` — strategia cache (network-first nawigacja + fallback, cache-first assety z odświeżeniem w tle, `/api/*` zawsze sieć) | **Bez zastrzeżeń** — poprawnie zaimplementowane i spójne z dokumentacją w nagłówku pliku | — |
| `core/sw_update.js` + `app_shell.js:14-93` — baner nowej wersji, `hardReloadApp()`, watchdogi iOS standalone, cykliczny check 20 min | **Bez zastrzeżeń** — zgodne z wcześniej wdrożonym rozwiązaniem znanego incydentu | — |
| `sw.js` `PRECACHE_URLS` nieaktualna | Patrz sekcja 5.1 | Średni |

---

## 6. Plan napraw — fazowany, z konkretnym zakresem

Kolejność faz odzwierciedla stosunek ryzyka do wartości: od najbezpieczniejszych/najtańszych do największych zmian architektonicznych. Każda pozycja = samodzielne zadanie do wykonania niezależnie.

### Faza 0 — szybkie wygrane (bardzo niskie ryzyko, wysoka wartość, każda 1 plik lub mechaniczna)

**STATUS: WYKONANE i WDROŻONE na prod 09.09.2026** (poza `creditReservationAdjustment` — celowo pominięta, komentarz w kodzie mówi że jest zachowana dla zgodności ze starymi danymi/skryptów naprawczych, nie jest martwym kodem w sensie przypadkowym). Zweryfikowane: build/lint/125 testów TS zielone, 248 testów Python zielone, syntax-check + brace-balance całego zmienionego frontendu, smoke test w przeglądarce na żywej prod (zero błędów konsoli, karty sprzętu renderują się poprawnie po realnym odświeżeniu SW, baner nowej wersji zadziałał). Przy okazji odkryto i naprawiono dodatkowo: martwy `.gearBadgesStack` w media query (420px) i zdublowany `.gearInlineMeta{display:none}` w media query (600px) w `gear.css` — nie było w oryginalnym planie, znalezione podczas finalnej weryfikacji grep. **Nowe, osobne (nienaprawione) znalezisko z tej sesji:** `.gearBadges` w `my_reservations_module.js:272` pokazuje status rezerwacji (aktywna/anulowana) i jest dziś niewidoczny (ta sama martwa reguła CSS, ale tu NIC tej informacji nie dubluje) — CSS `.gearBadges{display:none}` celowo pozostawiony nietknięty żeby nie zmieniać UI bez decyzji użytkownika; wymaga osobnej decyzji czy przywrócić widoczność.

| Zadanie | Plik(i) | Zmiana | Przyczyna |
|---|---|---|---|
| Podmienić placeholder sprzętu | `public/assets/kayak-placeholder.png` | Zastąpić plikiem <20 KB (SVG/skompresowany PNG) | 1,4 MB na każdą kartę sprzętu — sekcja 5.3 |
| Naprawić `.gearBadges` | `public/styles/gear.css:214-219` | Usunąć bazowe `display:none` (zostawić tylko wariant mobile jeśli intencja była taka) — **wymaga 1 decyzji produktowej: czy plakietka „Basen” ma być widoczna** | Realny bug UI — sekcja 5.4 |
| CSS `@import` → równoległe `<link>` | `public/index.html`, `public/styles/app.css` | 9 `@import` zamienić na 9 `<link rel="stylesheet">` w `<head>` | Blokujące sekwencyjne ładowanie — sekcja 5.3 |
| Zaktualizować przestarzały test | `functions/test/gear_core.test.ts` (linie ~50-52) | Zmienić oczekiwanie z `0` na realny koszt + sprawdzenie `waived:true` w warstwie wywołującej | Sekcja 0 — kontrakt zmieniony celowo |
| Usunąć martwy handler | `functions/src/api/gearKayaksListHandler.ts` | Usunąć plik (0 importów, nieosiągalny) | Sekcja 2.4 M1 |
| Usunąć 4 martwe funkcje backend | `godzinki_service.ts` (`creditReservationAdjustment`, `refundHoursForReservation`), `googleWorkspaceProvider.ts` (`listMembersOfGroup`), `googleSheetsProvider.ts` (`upsertMemberRowByEmail`) | Usunąć, 0 wywołań każda | Sekcja 3.4 |
| Usunąć martwy kod frontend | `render_shell.js:1673-1691` (`getModuleRouteByLabelOrId`) + ~13 bloków CSS w `gear.css`/`dashboard.css`/`kurs.css`/`start.css`/`godzinki.css`/`basen.css` (patrz 5.1) | Usunąć nieużywane funkcje/reguły | Sekcja 5.1 |
| Usunąć martwe artefakty repo | `ai_full_audit_report.txt`, `.json`; `.claude_context/`; `tools/build_project_context.py` | `git rm` | Sekcja 4.2 — 14,6 MB balastu w git |
| Usunąć nieużywaną zależność | `functions/package.json` | `npm uninstall @google-cloud/secret-manager` (+ opcjonalnie `firebase-functions-test`) | Sekcja 4.3 — 0 importów |
| Doczyścić log diagnostyczny | `gearReservationCreateHandler.ts:55-61` | Usunąć/obniżyć do `logger.debug` | Sekcja 2.2 P3 |

### Faza 1 — wysoki priorytet (niezawodność i wydajność ścieżek krytycznych)

**STATUS: WYKONANE i WDROŻONE na prod 09.09.2026.** Zweryfikowane: build/lint/125 testów TS zielone, deploy funkcji + nowych indeksów Firestore bez błędów (przejściowe 429 quota — auto-retry, wszystkie ostatecznie sukces), smoke test panelu Zarządu na żywej prod (sekcja "Prywatne kajaki — możliwe zaległości składkowe" poprawnie zwraca dane po refaktorze na wsadowe zapytanie), zero błędów konsoli.

**Ważne doprecyzowanie zapytań array-contains (sekcja 4.1 #3-7):** bezpośredni test na produkcyjnym Firestore (nie tylko analiza kodu) potwierdził jednoznacznie, że **żadne** z pięciu niepewnych zapytań (`gear_reservations` equality+array-contains ×2, `basen_enrollments` equality-only ×3) NIE wymaga composite index — wszystkie działają na istniejących indeksach jednopolowych. Nie dodano żadnych dodatkowych indeksów dla tych wzorców.

**Doprecyzowanie skali W2 (podwójny skan przy rejestracji):** dokładniejsza lektura kodu pokazała, że `findOpeningBalance`/`emailExistsInOtherObRow` NIE wykonują się przy każdym logowaniu każdego użytkownika (jak sugerował wstępny opis) — tylko dla użytkowników, którzy jeszcze nie mają `openingMatch` ustawionego (głównie nowi/niedopasowani użytkownicy). Naprawa i tak wdrożona (usuwa drugi zbędny skan tam, gdzie ta ścieżka się wykonuje).

| Zadanie | Plik(i) | Zmiana | Przyczyna |
|---|---|---|---|
| Dodać brakujące indeksy Firestore | `firestore.indexes.json` | Dodać `service_jobs(status, nextRunAt)` i `gear_reservations(userUid, createdAt desc)`; wdrożyć `firebase deploy --only firestore:indexes` | Sekcja 4.1 #1-2 — wysoka pewność, dotyczy każdego użytkownika/fallbacku jobów |
| Zweryfikować w konsoli Firebase brak `FAILED_PRECONDITION` dla wariantów array-contains | `getKayakReservationsHandler.ts:76-91`, `basen_service.ts` (5 zapytań) | Sprawdzić logi produkcyjne / spróbować w konsoli; dodać indeksy tylko jeśli faktycznie wymagane | Sekcja 4.1 #3-7 — niepewne bez dostępu do konsoli |
| Odzyskiwanie zawieszonych jobów | `service/worker/fallbackDailyWorker.ts` (+ ew. nowy plik) | Dodać zapytanie `status=="running" AND lockedUntil<now`, reset do `"queued"` przed właściwym fallbackiem | Sekcja 3.1 J1 — cichy, potencjalnie już aktywny problem. **Najpierw ręcznie sprawdzić Firestore pod kątem istniejących zawieszonych jobów** |
| Cache tokena OAuth | `service/providers/googleAuth.ts::getDelegatedAuth` | Dodać cache (Map) per `(delegatedUserEmail, scopes)` z TTL ~50 min | Sekcja 3.2 P1 — mnoży round-tripy w każdym syncu/powiadomieniu |
| Idempotencja wysyłki maili przy retry | `service/tasks/basenNotifySessionCancelled.ts`, `gearNotifyReservationCancelledByAdmin.ts`, `usersSyncFunctionRolesFromSetup.ts` | Dodać znacznik „wysłano do X” wzorem `remindersSentToUids` z `eventsNotifyUpcoming.ts`; w `usersSyncFunctionRolesFromSetup` zapisywać stan per-rola, nie zbiorczo | Sekcja 3.1 J2 — realne ryzyko podwójnych maili do członków |
| N+1 w panelu oczekujących | `getAdminPendingHandler.ts:305-318` | Zebrać `ownerContact` przed pętlą, rozwiązać właścicieli jednym `Promise.all`/`getAll` | Sekcja 2.1 W1 |
| Podwójny skan przy logowaniu | `registerUserHandler.ts:241,285` | `findOpeningBalance` ma zwracać snapshot i przekazywać do `emailExistsInOtherObRow` zamiast refetchować | Sekcja 2.1 W2 — najgorętsza ścieżka aplikacji |
| Timeout ręcznego uruchamiania tasków | `service/admin/adminRunTask.ts:15` | Podnieść `timeoutSeconds` do 300-540s lub przełączyć duże taski na kolejkowanie przez `service_jobs` | Sekcja 3.2 P7 |

### Poza planem — zgłoszone ad-hoc 09.09.2026

- **Kafelek „Sprzęt" na stronie głównej zawsze podświetlony** (`render_shell.js:197`) — dostawał klasę `primary` (niebieski akcent z `button.primary`, base.css) warunkowo przy `dash.canReserveGear`, czyli praktycznie zawsze dla zwykłych członków, sugerując mylnie stan "aktywny/wybrany". Usunięte — kafelek wygląda jak pozostałe. WYKONANE i WDROŻONE.

### Faza 2 — porządkowanie (duplikacje, spójność, średni priorytet)

**STATUS: WYKONANA w całości 09.09.2026, poza świadomie odłożonym dedup kart sprzętu.** Mechaniczna część (backend całość + frontend escapeHtml/escapeAttr/format_utils + sw.js precache) — wdrożone. Z 3 ryzykownych zmian UI: delegacja zdarzeń basen — WYKONANA i zweryfikowana na żywo; scalenie CSS modali gear/basen — pominięte po weryfikacji (nie była to czysta duplikacja); dedup kart sprzętu — odłożone do osobnej sesji (patrz tabela niżej).

**Wykonane (backend, zweryfikowane build/lint/125 testów + live):**
- `norm()`→import z `text_utils.ts` (23 pliki) + nowy `normNullish()` dla wariantu zachowującego falsy-a-zdefiniowane wartości (23 pliki) — **doprecyzowanie**: to NIE była przypadkowa duplikacja tylko dwa różne, każdy wewnętrznie spójne warianty; oba skonsolidowane do dwóch nazwanych helperów zamiast jednego
- `Promise.all(map(get))`→`db.getAll()` w 4 plikach (getBasenAdminGodzinyUsersHandler, getKayakReservationsHandler, getKursantStatsHandler, getAdminPendingHandler)
- Guard metody HTTP dodany w 11 plikach + ujednolicona kolejność (metoda→token) w 2 plikach (gearBundleReservationCreate/UpdateItemsHandler)
- Nowe wspólne moduły: `modules/shared/date_range_utils.ts` (`resolveDateRange` sparametryzowany per raport — **doprecyzowanie**: 4 raporty miały NAPRAWDĘ różne zachowanie (dostępność "current", domyślny zakres, definicja "miesiąca" 30dni vs kalendarzowy) — zachowane jawnie przez opcje, nie ujednolicone na siłę), `modules/shared/user_display.ts` (fullName/nickname/isRegistered), `CATEGORY_LABELS`/`VALID_CATEGORIES` w `gear_catalog_service.ts` — zastosowane w 10 plikach raportów/favorites
- `ClientError` w `basen_service.ts` (36 throw sites) + 8 handlerów `basen*` — zastąpiono kruche dopasowanie podłańcuchów (P4)

**Wykonane (frontend, zweryfikowane syntax-check + live na wszystkich sprawdzonych stronach):**
- `core/html_utils.js` (escapeHtml/escapeAttr) — 15 plików. **Przy okazji naprawiono realną lukę**: `gear_module.js`/`my_reservations_module.js` miały `escapeAttr` NIE escapujący pojedynczego cudzysłowu — teraz spójne pełne escapowanie wszędzie
- `core/format_utils.js` (formatDatePL/buildKayakTitle/formatShortDate/countReservationDays/pluralizeDays) — 7 plików. Ujednolicono drobny rozjazd fallbacku pustej daty ("—" wszędzie) — dotyczy tylko rzadkiej ścieżki błędu
- `sw.js` PRECACHE_URLS uzupełniony o 16 brakujących plików (w tym 2 nowe wspólne moduły)
- **Świadomie pominięte** (niższa wartość, wyższy koszt weryfikacji bez testów frontendu): `openMap()` duplikacja (3 pliki), kopiuj-do-schowka (2 pliki), dedup karty imprezy (render_shell.js vs impreza_module.js) — zostają jako drobne zaległości

| Zadanie | Plik(i) | Zmiana | Przyczyna |
|---|---|---|---|
| Konsolidacja `norm()` | ~25 plików backend + ~15 plików frontend `esc()` | Import ze wspólnego helpera zamiast lokalnych kopii | Sekcje 2.3 D1, 3.3 D1, 5.2 |
| Wspólne helpery dat/formatowania | Backend: `date_range_utils.ts` (4 pliki raportów); Frontend: `core/html_utils.js` + `core/format_utils.js` (~20 plików) | Wydzielić i podmienić importy | Sekcje 2.3 D2, 5.2 |
| `Promise.all(map(get))` → `db.getAll()` | `getBasenAdminGodzinyUsersHandler.ts`, `getKayakReservationsHandler.ts`, `getKursantStatsHandler.ts`, `getAdminPendingHandler.ts` | Ujednolicić do wzorca już istniejącego w 3 innych plikach | Sekcja 2.1 W6 / 2.5 N2 |
| Guard metody HTTP | 11 handlerów wymienionych w 2.2 P2 | Dodać identyczny 3-liniowy guard jak w pozostałych 48 | Sekcja 2.2 P2, 2.5 N3 |
| Typowany błąd basen | `basen_service.ts` + 8 handlerów `basen*` | `ClientError extends Error {status}` zamiast klasyfikacji po substringu | Sekcja 2.2 P4 |
| Refaktor rodziny raportów admina | 5 plików `getAdmin*ReportsHandler`/`getAdmin*RentalsHandler`/`getAdmin*ActivityHandler`/`getAdminMemberDuesHandler` | Wspólny szkielet `runReport()` + wspólny `resolveDateRange()` | Sekcja 2.5 N1 |
| ~~Delegacja zdarzeń w module basenu~~ **WYKONANE i WDROŻONE 09.09.2026** | `basen_module.js` | `bindSessionActions` (9 bloków `addEventListener`, rebindowanych przy KAŻDYM `refreshSessionsView`) → `bindSessionActionsDelegation`, bindowana RAZ w `render()` wzorem `bindModalCloseDelegation`. `sessions` (jedyna dana potrzebna spoza atrybutów klikniętego elementu) czytana z `innerEl._basenSessions`, odświeżanej przy każdym renderze. Zweryfikowane na żywo na prod: zapis→odświeżenie→klik na świeżo wyrenderowanym przycisku „Edytuj" działa przez ten sam nasłuchiwacz; dokładnie 1 zapytanie sieciowe na akcję (brak duplikacji) | Sekcja 5.3 |
| ~~Konsolidacja modala CSS~~ | `styles/gear.css` (`.gearModal*`) + `styles/basen.css` (`.basenModal*`) | **POMINIĘTE (decyzja 09.09.2026, po weryfikacji)** — okazało się że to NIE jest czysta duplikacja: różna szerokość (920px vs 640px), różny tryb layoutu treści (grid-centrowanie zdjęć vs flex-column formularza), różne wyrównanie przycisków akcji (space-between vs flex-end). Tylko 5 z 9 bloków reguł identyczne (~30 linii) — realna wartość konsolidacji zbyt mała wobec ryzyka wizualnej regresji formularzy basenu | Sekcja 5.2 — pierwotna ocena w raporcie była zbyt optymistyczna, nie sprawdzała rzeczywistych wartości właściwości |
| Deduplikacja renderowania kart sprzętu | `gear_module.js:1662-2160` (5 funkcji, ~500 linii) | Wspólny `renderGearCardShell()`, sparametryzować 5 funkcji kart | **ODŁOŻONE (decyzja 09.09.2026)** — czysta refaktoryzacja bez korzyści funkcjonalnej, w najbardziej złożonym pliku frontendu bez siatki bezpieczeństwa (brak testów/typów), wielokrotnie ręcznie dostrajanym pod realne zgłoszenia (dead layout, kategorie terenowe, warianty bez zdjęcia). Do zrobienia w osobnej sesji poświęconej wyłącznie temu, jedna kategoria na raz z pełnym testem wizualnym każdego wariantu (z/bez zdjęcia, uszkodzony/prywatny/basenowy) przed przejściem do kolejnej |
| `sw.js` precache — uzupełnić listę | `public/sw.js`, `scripts/bump-sw-cache.js` | Dodać 12 brakujących plików, rozważyć automatyczne generowanie listy | Sekcja 5.1 |

### Faza 3 — zmiany architektoniczne (do rozważenia długoterminowo, większy nakład)

| Zadanie | Plik(i) | Zmiana | Przyczyna |
|---|---|---|---|
| Archiwizacja zakończonych rezerwacji sprzętu | `gear_bundle_service.ts`, `reservation_limits.ts`, + nowy trigger/scheduler | Status `active→completed` po `endDate`; zapytania konfliktów filtrują tylko przyszłość/teraźniejszość | Sekcja 3.2 P2 — koszt rośnie bezpowrotnie z każdym rokiem klubu |
| Zdenormalizowane saldo godzinek/basenu | `godzinki_service.ts::computeNegativeBalances`, `getAdminMemberDuesHandler.ts`, `getBasenAdminGodzinyUsersHandler.ts` | Bieżące saldo per-user aktualizowane przy każdej transakcji ledgera, zamiast liczenia z pełnej historii | Sekcje 2.1 W4/W5, 3.2 P4 |
| Budowa testów dla obszarów 0% pokrycia | `gear_bundle_service.ts`, `reservation_limits.ts`, `basen_service.ts`, `km_*_service.ts`, `registerUserHandler.ts` | Wydzielić czyste funkcje logiki (konflikty, limity, bootstrap ról) i pokryć testami jednostkowymi | Sekcja 4.4 — najwyższe ryzyko regresji przy przyszłych zmianach |
| Cache danych modułu frontend | `core/app_shell.js` (ctx) + moduły z największym ruchem (Sprzęt, Start) | Prosty cache w pamięci z TTL dla list tylko-do-odczytu | Sekcja 5.3 |
| `AbortController` przy nawigacji | `core/api_client.js`, `core/render_shell.js`, sygnatura `render()` wszystkich modułów | Anulowanie zapytań przy zmianie widoku | Sekcja 5.3 — obecnie nieszkodliwe, ale kruche założenie |
| Upgrade zależności major | `functions/package.json` | eslint 8→10 + `@typescript-eslint` 5→8 (migracja do flat config), `typescript`, `firebase-admin`, `googleapis` | Sekcja 4.3 — version drift, nie pilne |
| Lazy-load CSS per moduł | `index.html`, `core/modules_registry.js`, `core/render_shell.js` | Ładować arkusz stylów modułu dopiero przy pierwszym wejściu, zamiast wszystkiego zawsze | Sekcja 5.3 — rozszerzenie Fazy 0 (na razie tylko `@import`→`<link>`) |

---

## 7. Statystyka końcowa

- **Plików przejrzanych w całości:** 61 (`functions/src/api/`) + 71 (`functions/src/modules/`+`service/`) + 44 (`public/`) = **176 plików źródłowych**, plus konfiguracja (`firestore.indexes.json`, `firebase.json`, `package.json` × 2).
- **Zweryfikowanych, konkretnych znalezisk:** 22 (backend API) + 19 (logika/joby) + ~18 (dane/indeksy/zależności, poza jakościowym opisem pokrycia testami) + 39 (frontend) = **~96 znalezisk**, wszystkie poparte odczytem aktualnego kodu lub grepem — zero hipotez podanych jako fakt.
- **Baza testowa:** build ✅, lint ✅, 124/125 testów TS ✅ (1 przestarzały), 248/248 testów Python ✅ (mirror-based, ryzyko rozbieżności z produkcją odnotowane w 4.4/sekcji 0).
- **Zero zmian w kodzie wykonanych w ramach tej analizy** — wszystko powyżej to plan do realizacji na życzenie, fazowany wg ryzyka/wartości.
